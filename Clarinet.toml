[project]
name = "tellor-table"
authors = ["Tellor Labs"]
description = "A blockchain-based asset tracking and verification system"
telemetry = false
requirements = []
[contracts.tracker]
path = "contracts/tellor-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
